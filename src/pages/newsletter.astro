---
export const prerender = false;

const destination = new URL("/blog/", Astro.url);
Astro.url.searchParams.forEach((value, key) => destination.searchParams.set(key, value));
return Astro.redirect(destination, 302);
---
